# 自定义表单组件
1. 组件配置中的字段配置，支持展开收起。
2. 支持7种类型（单行文本、多行文本、下拉选择、单选框、复选框、日期、数字）
3. **提交按钮**：文字、颜色、大小（小/中/大）
4. **重置按钮**：显示开关、文字、颜色
5. **布局设置**：按钮对齐（左/中/右）、圆角、字段间距
6. 数据交互
- **提交方式**：
  - 发送到API接口（配置URL、请求方式、认证）
  - 事件路由配置

- **提交成功后**：
  - 显示提示消息（可选重置表单）
  - 跳转到指定页面
  - 通过事件路由配置，触发其他微应用
- **提交失败后**：同上选项

# 搜索组件
1. 数据交互
- **提交方式**：
  - 发送到API接口（配置URL、请求方式、认证）
  - 事件路由配置
