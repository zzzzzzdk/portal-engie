———


  必改 1：把 styleMode和themeMode 从全局迁移到画布级


  - 目标：极简/标准（styleMode）和深色/浅色（themeMode）只作用当前画布
  - 改动点：
      - 把 styleMode和themeMode 从全局配置（ConfigStore）移出 “全局状态控制”
      - 改为存入 dashboardConfig（画布配置）
      - 发布/保存画布时写入 dashboardConfig.styleMode/themeMode
      - 画布加载时读取 dashboardConfig.styleMode/themeMode


  ———


  必改 2：风格/主题样式注入范围从”全局”改为”画布容器”


  - 目标：只影响画布组件，不影响 Header/系统 UI
  - 改动点：
      - 现有 styleTokens 和 themeTokens 是注入到 documentElement
      - 改为注入到”画布根容器 DOM”（dashboard 容器）
      - 深色/浅色（themeMode）和极简/标准（styleMode）均只作用于画布容器
      - 同一个系统可存在多个画布时，互不影响


  ———


  必改 3：极简/标准 & 深色/浅色切换 UI 改为画布级控制


  - 目标：用户明确知道这是”画布风格/主题”
  - 改动点：
      - 画布工具栏保留”标准 / 极简”和”深色 / 浅色”切换，但说明”仅影响当前画布”
      - 不再调用全局 setStyleMode / setThemeMode
      - 改为更新当前画布的 dashboardConfig.styleMode 和 dashboardConfig.themeMode


  ———


  必改 4：预览/发布不覆盖全局 styleMode / themeMode


  - 目标：避免预览覆盖系统风格和主题
  - 改动点：
      - 预览页加载画布时，不再写入全局 styleMode 和 themeMode
      - 只在画布容器内应用 dashboardConfig.styleMode 和 dashboardConfig.themeMode
      - 退出预览无需恢复全局风格/主题（因为没改）


  ———

    必改 5：全局系统 UI 固定浅色主题


  - 目标：themeMode 迁移到画布后，系统 UI 不再受画布主题影响
  - 改动点：
      - 页面头部（Header）固定浅色主题，不随画布 themeMode 变化
      - 微应用配置弹窗、应用列表等系统级 UI 固定浅色主题
      - 全局 ConfigStore 不再持有 themeMode，系统 UI 始终使用浅色

  ———


  调整项 1：微应用接收 styleMode / themeMode 的来源


  - 目标：子应用风格和主题与画布一致
  - 改动点：
      - 现在 styleMode 和 themeMode 均从全局传递
      - 改为从当前画布的 dashboardConfig.styleMode 和 dashboardConfig.themeMode 传递
      - 若画布未设置，styleMode 默认 normal，themeMode 默认 light


  ———


  调整项 2：styleTokens / themeTokens 计算逻辑


  - 目标：风格 token 和主题 token 只随画布变化
  - 改动点：
      - getStylePreset(styleMode, isDark) 仍可用
      - isDark 的来源改为画布的 dashboardConfig.themeMode（而非全局 themeMode）
      - 调用位置改为画布层（不存入全局）


  ———


  调整项 3：主题定制器 UI 提示


  - 目标：避免概念混淆
  - 改动点：
      - “主题定制器”仅保留主题预设/基础色等全局外观配置
      - 深色/浅色（themeMode）和极简/标准（styleMode）移到画布设置或工具栏，提示”仅本画布”
      - 全局 ConfigStore 移除 themeMode 和 styleMode，改由各画布的 dashboardConfig 管理


  ———


验证方案
切换画布深色/浅色模式 → Header 和��统 UI 保持浅色，只有画布区域变化
切换画布极简/标准风格 → 只有画布内 widget 样式变化
发布画布 → dashboardConfig 包含正确的 themeMode/styleMode
预览已发布画布 → 正确应用保存的主题配置，退出后系统状态不变
微应用 → 正确接收画布级 themeMode/styleMode
自动保存 → 正确保存画布级主题配置
新建空白画布 → 默认 themeMode: 'light', styleMode: 'normal'
加载已有画布 → 正确恢复保存的主题配置
运行 npx tsc --noEmit 确保无类型错误