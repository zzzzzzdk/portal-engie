接下来开始全局配置页面的开发，需求如下：页面布局为左右面板，左边是默认全局配置项，右侧是左侧选中的全局配置项的详情设置，默认不选中，右侧展示默认提示。 现在全局配置，暂时只需要三个模块:1.主题设计令牌（统一颜色、阴影与字体，作为平台视觉基线。）2.组件数据源（统一我的文档组件的数据来源）。3.消息文案字典（收敛确认框、占位提示等运行时文案）。

一. 主题设计令牌
1. 选中后右侧显示描述（
影响范围：组件背景和标题
影响策略：平台默认生效，允许业务主题覆盖；引用后优先影响视觉层，不直接
改变业务行为）
显示已经存的主题，可以新增、编辑，删除。这个好像需要二级展示了，帮我设计一下。

2. 每个主题支持的配置：主题方案名称、页面背景设置、组件背景设置、组件标题设置。

3. 新增主题：每套主题包含主题方案、名称、右侧具有新增主题按钮。

4.删除主题：支持删除，给二次确认，至少保留一个主题方案，只剩下一个方案时不允许删除，如果工作台已引用被删除的方案，自动切换到剩余的第一个方案，删除的时候给出提醒：若工作台已引用当前方案，将会自动切换到首个主题方案！是否确认删除! 

5.组件背景设置、组件标题设置、页面背景设置，建议直接复用项目中工作台中，组件配置中的背景和标题设置，保证功能统一，页面背景设置同理

二、组件数据源
1. 选中后右侧显示描述（
组件数据源配置
影响范围：我的文档组件数据源配置
影响策略：配置保存立即生效）

2.配置包括：
组件数据源配置（表单展示）
业务API地址（tooltip： 文档组件调用“业务API”的根地址）
文档存储服务地址（tooltip：文档文件存储服务的访问地址）

三、消息文案字典
1. 选中后右侧显示描述（
消息文案配置
影响范围：自定义表单保存
影响策略：平台默认兜底，业务可覆盖文案）

2.配置包括：
消息文案设置（表单展示）
文案key - 默认文案


主题字段我已经和现有工作台配置字段对齐了，页面背景走 backgroundType/backgroundColor/backgroundImage/backgroundGradient...，组件标题走 showTitle/titleColor/titleFontSize/titleFontWeight，后续尽量直接映射，不要再包一层新结构。
需要先明确优先级：全局主题默认值 < 工作台级主题覆盖 < 单组件手动配置覆盖。这个优先级不定，后面一定会互相打架。
页面背景要接到工作台的 dashboardConfig，组件背景和标题要接到组件或分组渲染层；如果只接编辑态、不接预览态，显示会不一致。
主题删除时不仅要处理当前编辑页，还要处理已发布工作台、历史快照、预览页引用的兜底切换，否则会出现主题 id 丢失。
组件数据源目前是“配置管理 + mock 保存”，还没有真正改工作台运行时请求来源。尤其“我的文档”现在走 fileManager.ts 和 useFileStore.ts，后续接入要从这里下手。
getMinioApiUrl() 和 axios 实例是模块初始化时确定的，如果要做到“保存立即生效”，不能只改配置值，还要补运行时更新请求基址的机制。
消息文案后续接入自定义表单时，建议统一读取顺序：业务覆写文案 > 全局配置文案 > 组件内默认文案，避免散落硬编码。
背景图片如果要用于发布预览，需确认预览页、编辑页、mock/正式环境的图片地址和 CORS 都一致，否则会出现编辑态可见、预览态不可见。


现在想要将全局配置的功能对接到工作台，需求：
1. 主题设计令牌中的保存的主题需要在工作台设置页面背景设置、组件背景设置、组件标题设置时，提供一个是否引用全局配置的开关，如果选择开启，则需要选择主题设计方案，方案的选项就是在全局配置中已经保存的方案，选择后自动填充到组件设置中，比如选择的主题设计方案中设置了组件的背景颜色为黑色，选择了后在此将会在背景颜色部分自动填充为何全局配置方案一样的内容 ，引用后无法修改，要修改需要取消引用说明：其他组件涉及到主题令牌设计相关的，也是这个交互逻辑，同步修改其他组件。取消引用之后，之前填入的配置相关不用清空，用户可能会基于这个配置进行修改。
2. 主题切换，不是放在公共的区域，而是放在每个组件配置里（页面设置也是），参考原型图。
3. 组件数据源：我的文档组件采用此处设置的数据源相关配置，不采用全局变量。
4. 消息文案配置，具有默认值，工作台相关的组件，默认填入此配置，用户可以进行修改。
请先列出修改方案，先让我确认，再进行开发。
   
