
前端代码：D:\intelligentComputing\portal-engine
后端代码：D:\intelligentComputing\PortalEngie_Backend

一. 需要给应用列表修改为“暂存”/“待发布更新”/“已发布”三种状态。用户新增页面，在工作台点击保存之后，应用列表显示“暂存”状态，在工作台点击发布之后，应用列表显示“已发布”状态。然后在应用列表点击“已发布”的数据进行编辑，编辑之后在工作台点击保存，变为“待发布更新”状态；点击发布，变为“已发布”状态。

新增了设置为首页按钮。

需求描述：
1. 如果是草稿状态，支持预览、编辑、导出和删除
2. 如果是待发布更新状态（表示已发布了一个版本然后又编辑了），支持设置为首页（已发布版本）、分享（已发布版本）、编辑（编辑草稿版本）、查看、导出、删除（需要选择删除草稿还是发布版本）
3. 如果是已发布状态，支持设置为首页、分享、查看、编辑、导出、删除
4. 只有已发布才会生成分享链接，暂存状态没有分享链接，以发布动作作为更新触发点，例如当前接口已经被其他业务系统调用，此时进行修改了，修改内容不会被推送出去，只有再次点击发布时才会替换掉上一个已经发布的版本（由此来保证修改期间不会影响线上环境）。
5. 只有为已发布的状态才可以设置为首页（设置为首页的目的是考虑到其他系统默认在部署时带着，但是用户可能自行修改页面，修改了页面之后也将其设置为系统首页，设置为首页之后下次再访问会跳转到用户设置的首页），点击设置为首页给二次确认，并提示：是否将当前页面设置为门户首页，设置后将会替换掉默认的首页。
6. 需要添加一个路由，比如homepage之类的，请合理命名，我想的是访问这个路由，会请求设置好的首页。这样其他系统访问的时候，就固定访问这个路由就好了，具体显示什么页面，本页面逻辑进行处理。


二. 应用列表页添加导入按钮。应用卡片导出功能，添加导出为json的选项。

需求描述：
1. 支持导入json包直接生成页面，导入成功后直接生成页面到工作台的页面上
2. 同样，导出也支持导出为json包


以上功能为产品提的理想状态需求，需要你帮我看能否实现，怎么实现比较好，给出建议。
如果不能实现，给出他可以修改的合理方案。
如果可以实现，帮我列出详细的实现方案。
如果哪里有疑问点，请及时提出。