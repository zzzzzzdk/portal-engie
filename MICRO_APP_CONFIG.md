# 微应用配置页面使用说明

## 功能概述

微应用配置页面提供了一个可视化的界面，用于管理 `public/config/micro-apps.json` 文件中的微应用配置。

## 访问路径

- **路由**: `/micro-app-config`
- **入口**: 在开发环境下，Layout 头部右侧有一个设置图标（齿轮），点击即可进入配置页面

## 主要功能

### 1. 系统管理

- **添加系统**: 点击右上角"添加系统"按钮
- **编辑系统**: 点击系统行的"编辑"按钮
- **删除系统**: 点击系统行的"删除"按钮
- **展开/折叠**: 点击系统行可展开查看该系统下的所有模块

#### 系统字段说明

- **系统ID**: 唯一标识符（如: `system-finance`）
- **系统名称**: 显示名称（如: `财务系统`）
- **描述**: 系统的详细描述
- **图标**: Ant Design 图标名称（如: `AccountBookOutlined`）
- **分类**: 系统分类标签（如: `业务系统`）

### 2. 模块管理

在展开的系统行内可以管理模块：

- **添加模块**: 点击"添加模块"按钮
- **编辑模块**: 点击模块卡片右上角的编辑图标
- **删除模块**: 点击模块卡片右上角的删除图标

#### 模块字段说明

- **模块ID**: 唯一标识符（如: `finance-report`）
- **模块名称**: 显示名称（如: `财务报表`）
- **描述**: 模块的详细描述
- **访问URL**: 微应用的访问地址（如: `http://localhost:3001/#/report`）
- **入口地址**: 微应用的入口地址（如: `http://localhost:3001/`）
- **图标URL**: 模块图标地址（可选）
- **默认尺寸**:
  - **宽度 (w)**: 1-12 格（默认 6）
  - **高度 (h)**: 最小 1 格（默认 4）

### 3. 事件管理

每个模块支持两种类型的事件：

#### 可发送事件 (Emittable Events)

该模块可以向外发送的事件：

- **添加**: 点击"添加可发送事件"按钮
- **删除**: 点击事件标签的关闭图标

#### 可监听事件 (Listenable Events)

该模块可以监听的事件：

- **添加**: 点击"添加可监听事件"按钮
- **删除**: 点击事件标签的关闭图标

#### 事件字段说明

- **事件类型**: 事件的唯一标识符（如: `data:submit`）
- **事件名称**: 显示名称（如: `数据提交`）
- **描述**: 事件的详细描述

### 4. 配置导入/导出

#### 导出配置

点击右上角"导出配置"按钮，会下载当前配置为 `micro-apps.json` 文件。

#### 导入配置

1. 点击"导入配置"按钮
2. 选择本地的 `micro-apps.json` 文件
3. 配置会立即加载到页面中

> **注意**: 导入配置后，需要手动保存到服务器（目前仅支持客户端预览）

## 配置示例

```json
{
  "version": "1.0.0",
  "apps": [
    {
      "id": "system-finance",
      "name": "财务系统",
      "description": "企业财务管理系统",
      "icon": "AccountBookOutlined",
      "category": "业务系统",
      "modules": [
        {
          "id": "finance-report",
          "name": "财务报表",
          "description": "查看和生成财务报表",
          "url": "http://localhost:3001/#/report",
          "entry": "http://localhost:3001/",
          "icon": "http://localhost:3001/assets/icon.png",
          "defaultSize": {
            "w": 6,
            "h": 4
          },
          "emittableEvents": [
            {
              "type": "data:submit",
              "name": "数据提交",
              "description": "提交财务数据"
            }
          ],
          "listenableEvents": [
            {
              "type": "data:refresh",
              "name": "数据刷新",
              "description": "刷新财务数据"
            }
          ]
        }
      ]
    }
  ]
}
```

## 技术实现

### 文件结构

```
src/components/MicroAppConfigPage/
├── index.tsx       # 主组件
└── index.scss      # 样式文件
```

### 关键特性

1. **实时配置加载**: 页面加载时自动从 `/config/micro-apps.json` 读取配置
2. **表格展开行**: 使用 Ant Design Table 的 `expandable` 特性展示模块
3. **表单验证**: 所有必填字段都有验证规则
4. **卡片布局**: 模块使用响应式网格卡片展示
5. **事件管理**: 使用 Tabs 组件分别管理可发送和可监听事件

### 权限控制

- 配置页面入口仅在**开发环境**下显示（通过 `isDevelopment()` 判断）
- 生产环境下用户无法访问配置页面

## 待实现功能

目前配置仅支持客户端预览，后续可添加：

1. **保存到服务器**: 添加 API 接口将配置保存到 `public/config/micro-apps.json`
2. **配置历史**: 保存配置的历史版本，支持回滚
3. **配置验证**: 添加 URL 可访问性验证
4. **拖拽排序**: 支持拖拽调整系统和模块的顺序
5. **批量导入**: 支持从 Excel 或 CSV 批量导入配置

## 注意事项

1. **系统ID和模块ID唯一性**: 确保每个系统和模块的 ID 在整个配置中是唯一的
2. **URL格式**: 确保访问URL和入口地址格式正确，包含协议（http/https）
3. **默认尺寸**: 宽度建议在 4-8 格之间，高度根据实际内容调整
4. **离线环境**: 图标URL应指向内网可访问的地址
